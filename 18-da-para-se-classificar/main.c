#include <stdio.h>

int main() {
	// Escreva um programa que leia 3 variáveis e informe se o time pode continuar jogando ou não.

	int points, remainMatches, minPoints;
	scanf("%d %d %d", &points, &remainMatches, &minPoints);


	return 0;
} 
